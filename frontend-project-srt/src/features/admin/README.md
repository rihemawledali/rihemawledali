# Admin Frontend Features

Purpose: admin-facing management pages.

Subfeatures:

- `dashboard`: global overview.
- `adhesions`: membership request approval workflow.
- `users`: user CRUD.
- `suppliers`: supplier CRUD.
- `conventions`: convention CRUD.
- `offers`: purchase orders and tickets.

External imports should prefer the subfeature `index.ts` files.
